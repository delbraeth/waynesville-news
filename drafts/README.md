# (deprecated)

Drafts now live in the content collection as **unpublished briefs**
(`src/content/briefs/*.md` with `published: false`) and are published entirely on
github.com — see **PUBLISHING.md** in the repo root. This `drafts/` folder is no
longer used and can be deleted.
